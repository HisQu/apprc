[build-system]
requires = ["uv_build>=0.11.16,<0.12"]
build-backend = "uv_build"

[project]
name = "apprc_example_app"
version = "0.0.0"
description = "Repository-local example app for exercising AppRC."
requires-python = ">=3.12"
dependencies = [
    "apprc",
]

[project.scripts]
apprc = "apprc_example_app.cli:main"

[tool.uv.build-backend]
module-name = "apprc_example_app"
module-root = "src"
