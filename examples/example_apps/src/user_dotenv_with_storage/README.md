# User dotenv with storage example

This app declares both persistent capabilities. Setup creates
`apprc.user.env`, the central `apprc.toml` registry, and a storage-local
`apprc.storage.env`.

```shell
apprc-examples-lab user-dotenv-with-storage
```
