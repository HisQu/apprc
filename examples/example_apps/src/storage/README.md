# Storage example

This app declares `storage=rc.Storage()` and no user dotenv. Setup creates the
central `apprc.toml` registry and one storage-local `apprc.storage.env`.

```shell
apprc-examples-lab storage
```
