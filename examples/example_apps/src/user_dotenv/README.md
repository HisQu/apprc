# User dotenv example

This app declares `user_dotenv=rc.UserDotenv()` and no storage. Setup creates
one `apprc.user.env`; the editor and `config set --scope user` write only that
file.

```shell
apprc-examples-lab user-dotenv
```
