# Process environment example

This app declares typed settings but no managed user files. It reads process
environment values and Python defaults. Its generated `config` group offers
read-only diagnostics plus cleanup of stale AppRC files.

```shell
apprc-examples-lab process-env
```
